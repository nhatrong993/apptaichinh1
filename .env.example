# ============================================================
# CRYPTO TREND HUNTER — Environment Variables
# ============================================================
# Copy file này thành .env.local rồi điền API keys của bạn.
# Chạy: copy .env.example .env.local
# ============================================================

# ---- CoinGecko API ----
# Đăng ký FREE Demo Key tại: https://www.coingecko.com/en/api
# Free tier: 30 calls/phút, 10,000 calls/tháng
COINGECKO_API_KEY=

# ---- Twitter/X API v2 ----
# Đăng ký tại: https://developer.x.com/en/portal/dashboard
# LƯU Ý: Free tier chỉ cho 100 reads/tháng (gần như vô dụng)
# Cần Basic tier ($200/tháng) để search tweets
# Nếu để trống, hệ thống sẽ bỏ qua Twitter data
TWITTER_BEARER_TOKEN=

# ---- Google Trends ----
# Không cần API key — dùng google-trends-api npm package
# Tự động hoạt động, không cần cấu hình gì
